# EntryValidation
A local web application for validating tabular data files (CSV, Excel, JSON, XML) against configurable schemas.  Upload a file, configure column types and rules, run validation, and download annotated results.
